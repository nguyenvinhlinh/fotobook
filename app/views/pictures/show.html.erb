<p id="notice"><%= notice %></p>
<p>
    <strong>Tags:</strong>
    <% for counter in 0...@picture.tags.size %>
        <a href="/tags/<%=@picture.tags[counter].tag %>">
            <% if counter == @picture.tags.size - 1 %>
                <%= @picture.tags[counter].tag %>
            <% else %>
                <%= @picture.tags[counter].tag %>, 
            <% end%>
        </a>
    <% end  %>
</p>

<p>
  <img class="img-responsive"src="<%= @picture.url %>"/>
</p>
<% if @is_belong_to_current_user %>
    <%= link_to 'Edit', edit_users_picture_path(@picture) %> |
    <%= link_to 'Delete', "/users/pictures/#{@picture.id}", :method => :delete, :data => {:confirm => "Are you sure ?"} %> |
<% end %>
<%= link_to 'Back', pictures_path %>

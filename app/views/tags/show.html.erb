<p id="notice"><%= notice %></p>

<p>
  <strong>Tags:</strong> <%= @tag.tag %> <br/>
  <% @tag.pictures.each do |f| %>
      <img class="img-responsive" src="<%= f.url%>" />
  <% end %>
</p>
<%= link_to 'Back', {controller: "tags", action: "index"}%>
